apiVersion: v1
kind: Service
metadata:
  name: questlog
  namespace: questlog
  labels: {app: questlog}
spec:
  selector: {app: questlog}
  ports:
    - name: http
      port: 8080
      targetPort: http
    - name: metrics
      port: 9090
      targetPort: metrics
