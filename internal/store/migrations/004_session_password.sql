-- Отпечаток пароля в сессии: сменили пароль — все старые входы перестают действовать.
-- Старые сессии получают пустой отпечаток и становятся недействительными (нужно войти один раз заново).
ALTER TABLE sessions ADD COLUMN password_fp text NOT NULL DEFAULT '';
